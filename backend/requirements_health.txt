# Token Health System - Required Dependencies
# Install with: pip install -r requirements_health.txt

# Core dependencies (already in main requirements.txt)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
requests>=2.31.0
python-dotenv>=1.0.0

# New dependencies for comprehensive health system
anthropic>=0.18.0  # For LLM summaries (optional but recommended)

# Note: anthropic is optional - the system will work without it,
# but you won't get AI-powered summaries
